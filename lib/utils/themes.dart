import 'package:anti_rigging/utils/colors.dart';
import 'package:flutter/material.dart';

class AppThemes {
  final theme = ThemeData(useMaterial3: true, colorScheme: ColorScheme.fromSeed(seedColor: primaryColor));
}
